# Development workflow artifacts kept local or in separate tooling repo
# Dev tooling and agent workflow — lives in fine-rag-devtools private repo
.ai/
.github/
scripts/
tools/

# Planning and design docs — lives in fine-rag-devtools private repo
docs/epics/
docs/architecture/
docs/distilled_requirements*.md
docs/system_design*.md
docs/workflow_review.md
docs/workflow_review_guide.md

# Cloud deployment and host-specific proxy files (local only; never commit)
run_stack_with_tunnel.sh
cloud-apps.conf
setup_cloud_proxy.sh

# Local Docker secrets material
secrets/*.txt

# Environment variable files (may contain credentials)
.env
.env.*
*.env
